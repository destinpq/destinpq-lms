# Database connection string - for local development
DATABASE_URL=postgresql://pratik:tiger@localhost:5432/lms_db

# Application port
PORT=3001

# Environment
NODE_ENV=development

# JWT secret for authentication
JWT_SECRET=your_secure_jwt_token_secret_key

# Database configuration for TypeORM
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=pratik
DB_PASSWORD=tiger
DB_DATABASE=lms_db

# For production, use the following settings:
# NODE_ENV=production
# DATABASE_URL comes from Heroku PostgreSQL add-on
# Set DB_SSL=true for Heroku PostgreSQL connections 