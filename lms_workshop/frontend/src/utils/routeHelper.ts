/**
 * A simple route helper utility to construct application routes
 */

/**
 * Get the full route path with proper prefixing if needed
 * @param path The relative path
 * @returns The full route path
 */
export const getRoute = (path: string): string => {
  // Simply return the path as is, no prefixing needed
  return path;
}; 