'use client';

import React, { ReactNode } from 'react';

/**
 * A utility component to fix object rendering issues
 * (placeholder for the deleted component)
 */
export function ObjectRenderingFixer({ children }: { children?: ReactNode } = {}) {
  // This is just a pass-through component since we're creating a placeholder
  return <>{children}</>;
} 